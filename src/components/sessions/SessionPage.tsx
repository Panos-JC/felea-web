import React from "react";
import { Layout } from "../layout/Layout";

interface SessionPageProps {}

export const SessionPage: React.FC<SessionPageProps> = ({}) => {
  return <Layout maxWidth="sm">Session</Layout>;
};
