query Employees($companyId: Int!) {
  employees(companyId: $companyId) {
    errorMsg
    data {
      id
      firstName
      lastName
      sessionRequestsCount
      user {
        email
        avatar
      }
    }
  }
}
