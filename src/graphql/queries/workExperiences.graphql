query WorkExperiences($mentorId: Int) {
  workExperiences(mentorId: $mentorId) {
    errorMsg
    data {
      id
      role
      companyName
      from
      untill
      createdAt
      updatedAt
      description
      industries {
        name
      }
    }
  }
}
