query WorkExperiences($mentorId: Int!) {
  workExperiences(mentorId: $mentorId) {
    id
    role
    companyName
    from
    untill
    createdAt
    updatedAt
    description
    industries {
      name
    }
  }
}
