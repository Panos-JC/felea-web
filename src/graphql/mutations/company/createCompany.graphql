mutation CreateCompany($input: CreateCompanyInput!) {
  createCompany(input: $input) {
    errorMsg
    company {
      id
      name
      boughtAccounts
      remainingAccounts
      code
    }
  }
}
