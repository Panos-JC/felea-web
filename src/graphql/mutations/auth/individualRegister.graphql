mutation RegisterIndividual($options: IndividualRegisterInput!) {
  registerIndividual(options: $options) {
    errors {
      field
      message
    }
    user {
      email
      activated
    }
  }
}
