mutation SetMentorDetails($options: MentorDetailsInput!) {
  setMentorDetails(options: $options) {
    errorMsg
    mentor {
      firstName
      lastName
      title
      location
      languages
      rate
    }
  }
}
